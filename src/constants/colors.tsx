interface Palettes {
    primary: string;
    background: string;
    grey: string;
    black: string;
}

// tagged union

const palettes: Palettes = {
    primary: "#017ef4",
    background: "#f8f8f8",
    grey: "#f1f1f1",
    black: "#444444",
};

export default palettes;
