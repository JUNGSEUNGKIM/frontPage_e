const primary: string = "#017ef4";
const bg: string = "#fefefe";
const grey: string = "#f2f4f6";
export { primary, bg, grey };

interface Palettes {
    primary: string;
    background: string;
    grey: string;
}

const palettes: Palettes = {
    primary: "#017ef4",
    background: "#fefefe",
    grey: "#e8e8e8",
};

export default palettes;
